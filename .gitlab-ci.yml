variables:
  GIT_SUBMODULE_STRATEGY: recursive

pages:
  image:
    name: klakegg/hugo:ext-alpine
    entrypoint : ['']
  before_script:
    - git submodule update --init --recursive
  script:
    - hugo --gc --minify
    - find public -type f -exec gzip -f -k 9 \;
    - find public -type f -exec brotli -f -k 9 \;
  artifacts:
    paths:
      - public
  only:
    - main
